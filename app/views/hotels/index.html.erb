<p id="notice"><%= notice %></p>

<h1>Raservation Room</h1>

<table>
  <thead>
    <tr>
<!--       <th>Customer</th> -->
      <th>Floor</th>
      <th>Room</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @hotels.each do |hotel| %>
      <tr>
     <td><%= hotel.customer %></td>
        <td><%= hotel.floor %></td>
        <td><%= hotel.room %></td>
        <td><%= link_to 'Show', hotel %></td>
        <td><%= link_to 'Edit', edit_hotel_path(hotel) %></td>
        <td><%= link_to 'Destroy', hotel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Reservation', new_hotel_path %>
